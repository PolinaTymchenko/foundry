---
"create-react-foundry": minor
---

Scaffolds an `apps/web` app alongside Storybook: a Vite + React shell wired to `packages/react`, with an optional TanStack Query/Form/Table demo backed by a mock API (`apps/web/src/api/tasks.ts`) that's safe to delete if you don't want it. `pnpm dev` now opens both the app (`:5173`) and Storybook (`:6006`).
