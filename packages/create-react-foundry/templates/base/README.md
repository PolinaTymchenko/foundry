# {{projectName}}

Scaffolded with [Foundry](https://github.com/foundry-ui/foundry).

This is an early-stage scaffold: a working pnpm + Turborepo monorepo with no
packages in it yet. Add your first package under `packages/`, then:

```
pnpm install
pnpm build
pnpm test
pnpm lint
pnpm typecheck
```

Remember to add a `LICENSE` that fits your project — Foundry doesn't choose
one for you.
